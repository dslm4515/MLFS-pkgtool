# Stage1: Packaging
# Build and install under chroot
#
# Required by to build patched glib

# Build
pip3 wheel -w dist --no-cache-dir --no-build-isolation --no-deps $PWD

# Install to llvmtools
pip3 install --no-index --no-user --find-links dist packaging

