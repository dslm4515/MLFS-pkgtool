# Final System - lz4 64-bit & 32-bit
# Build and install under chroot

# Setup variables for packaging:
export PNM="lz4"
export  PV="1.10.0"

# Compile the package: 
make PREFIX=/usr

# To test the results, issue: 
make -j1 check

# Install to the staging area : 
make PREFIX=/usr DESTDIR=$BUILD install

# Build the 64-bit pacakage
cd ${BUILD} && mkdir -v ${BUILD}/install         && \
cp -v $DESCS/${PNM} ${BUILD}/install/slack-desc  && \
makepkg -l y -c n ${PKGS}/${PNM}-${PV}-${PF64}

# Install to system
spkg ${PKGS}/${PNM}-${PV}-${PF64}

# Clean staging area
if [ $BUILD ]; then rm -rf ${BUILD}/* ; fi

# Return to source tree
cd -

## 32-bit lz4 build

# Clean previous build: 
make clean

# Compile the 32-bit libraries: 
CC="gcc -m32" make

# Install the 32-bit libraries to the staging area: 
make PREFIX=/usr       \
     LIBDIR=/usr/lib32 \
     DESTDIR=$(pwd)/m32 install
mkdir -pv $BUILD/usr/lib32
cp -a m32/usr/lib32/* $BUILD/usr/lib32/

# Build the 32-bit pacakage
cd ${BUILD} && mkdir -v ${BUILD}/install         && \
cp -v $DESCS/${PNM}_32 ${BUILD}/install/slack-desc  && \
makepkg -l y -c n ${PKGS}/${PNM}_32-${PV}-${PF32}

# Install to system
spkg ${PKGS}/${PNM}_32-${PV}-${PF32}

# Clean staging area
if [ $BUILD ]; then rm -rf ${BUILD}/* ; fi
