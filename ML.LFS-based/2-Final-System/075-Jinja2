# Final System - Jinja2   
# Build and install under chroot

# Setup variables for packaging:
export PNM="Jinja2"   
export  PV="3.1.6" 

# Compile the package: 
pip3 wheel -w dist --no-cache-dir --no-build-isolation --no-deps $PWD

# Install to the staging area:
pip3 install --no-index --find-links dist Jinja2 --root=$BUILD

# Build the pacakage
cd ${BUILD} && mkdir -v ${BUILD}/install         && \
cp -v $DESCS/${PNM} ${BUILD}/install/slack-desc  && \
makepkg -l y -c n ${PKGS}/${PNM}-${PV}-${PFNA}

# Install to system
spkg ${PKGS}/${PNM}-${PV}-${PFNA}

# Clean staging area
if [ $BUILD ]; then rm -rf ${BUILD}/* ; fi

