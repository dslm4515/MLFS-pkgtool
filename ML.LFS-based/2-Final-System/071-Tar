# Final System - Tar 
# Build and install under chroot

# Setup variables for packaging:
export PNM="tar"
export  PV="1.35"

# Per LFS: First fix the package to 
# build with acl-2.4.0:
patch -Np1 -i ../patches/tar-ml.lfs/tar-1.35-acl_fix-1.patch

# Prepare tar for compilation: 
FORCE_UNSAFE_CONFIGURE=1 \
./configure --prefix=/usr

# Compile the package: 
make 

# Install to the staging area:
make DESTDIR=$BUILD install

# Build the pacakage
cd ${BUILD} && mkdir -v ${BUILD}/install         && \
cp -v $DESCS/${PNM} ${BUILD}/install/slack-desc  && \
makepkg -l y -c n ${PKGS}/${PNM}-${PV}-${PF64}

# Install to system
spkg ${PKGS}/${PNM}-${PV}-${PF64}

# Clean staging area
if [ $BUILD ]; then rm -rf ${BUILD}/* ; fi

